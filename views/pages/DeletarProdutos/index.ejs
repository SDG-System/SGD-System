<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Excluir Produtos</title>
    <link rel="stylesheet" href="/css/principal.css" />
    <link rel="stylesheet" href="/css/sidebar.css" />
    <link rel="stylesheet" href="/css/delete.css" />
    <%- include('../partials/head') %>
  </head>  
  <style>
     body {
      background-color: #f1f4f9;
      color: #333;
      font-family: Arial, sans-serif;
  }
  </style>
  <body>
    <%- include('../partials/sidebar') %>
    <div class="content">
      <h1 class="mb-4">Apagar Produtos</h1>
      <div class="row">
        <% deletar.forEach(deleta => { %>
          <div class="col-md-4">
            <div class="card mb-4">
              <div class="card-body text-center">
                <h5 class="card-title">Código: <%= deleta.cd_produto %></h5>
                <p class="card-text">Nome: <%= deleta.nm_produto %></p>
                <p class="card-text">Descrição: <%= deleta.ds_produto %></p>
                <p class="card-text">Preço: R$ <%= deleta.vl_produto %></p>
                <p class="card-text">Quantidade: <%= deleta.qt_produto %></p>

                <!-- Botão para abrir o modal de confirmação -->
                <button class="btn btn-danger" data-toggle="modal" data-target="#confirmDeleteModal"
                        data-id="<%= deleta.cd_produto %>" data-descricao="<%= deleta.nm_produto %>">
                  <i class="bi bi-trash"></i> Excluir
                </button>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    </div>

    <!-- Modal de Confirmação -->
    <div class="modal fade" id="confirmDeleteModal" tabindex="-1" role="dialog" aria-labelledby="confirmDeleteModalLabel"
         aria-hidden="true">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="confirmDeleteModalLabel">Confirmar Exclusão</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
          <div class="modal-body">
            Você tem certeza que deseja excluir o produto <strong id="idProdutoText"></strong>?
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
            <form id="deleteForm" method="post" autocomplete="off">
              <input type="hidden" id="idProduto" name="idProduto" value="">
              <button type="submit" class="btn btn-danger">Apagar</button>
            </form>
          </div>
        </div>
      </div>
    </div>

    <!-- Scripts -->
    <script src="./js/DeletarProdutos/index.js"></script>
    <script src="/js/Logout/index.js"></script>
    <script src="/js/Sidebar/index.js"></script>
  </body>
</html>  
