<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Atualização de Produtos</title>
  <link rel="stylesheet" href="/css/principal.css" />
  <link rel="stylesheet" href="/css/sidebar.css" />
  <link rel="stylesheet" href="/css/atualizarEntrada.css" />
  <%- include('../partials/head') %>
</head>
<body>
  <%- include('../partials/sidebar') %>
  <div class="content">
    <div class="titulo">
      <h1>Atualização de Produtos</h1>
    </div>
    <div class="container">
      <div class="col-md-6">
        <div class="card">
          <div class="card-body">
            <form method="post" id="formEntrada" autocomplete="off">
              <h5 class="card-title" style="color: #000">
                <span id="codigoProduto" class="editable" data-id="<%= produto.cd_produto %>"><%= produto.cd_produto %></span>
              </h5>
              <div class="form-group">
                <input
                  type="text"
                  id="idProduto"
                  name="idProduto"
                  class="form-control"
                  placeholder="Código do Produto"
                  value="<%= produto.cd_produto %>"
                  required
                />
              </div>
              <div class="form-group">
                <input
                  type="text"
                  id="quantidadeProduto"
                  class="form-control"
                  name="quantidadeProduto"
                  placeholder="Quantidade"
                  value="<%= produto.quantidade %>"
                  required
                />
              </div>
              <button type="submit" class="btn btn-dark btn-block">Atualizar</button>
            </form>
          </div>
        </div>
      </div>
    </div>

    <!-- Modal de Confirmação -->
    <div class="modal fade" id="confirmModal" tabindex="-1" aria-labelledby="confirmModalLabel" aria-hidden="true">
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="confirmModalLabel">Confirmação</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
          <div class="modal-body">
            Atualização realizada com sucesso!
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-primary" id="confirmBtn">OK</button>
          </div>
        </div>
      </div>
    </div>

    <!-- Modal de Erro -->
    <div class="modal fade" id="errorModal" tabindex="-1" aria-labelledby="errorModalLabel" aria-hidden="true">
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="errorModalLabel">Erro de Atualização</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
          <div class="modal-body">
            <p id="error-message">Erro ao atualizar o produto. Verifique as informações inseridas.</p>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-danger" id="errorBtn">OK</button>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="/js/Logout/index.js"></script>
  <script src="/js/Sidebar/index.js"></script>
  <script src="/js/AtualizarEntrada/index.js"></script>
</body>
</html>
